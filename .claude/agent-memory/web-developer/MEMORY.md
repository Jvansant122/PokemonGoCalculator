# Web-Developer Memory Index

- [Wiring persistsThroughFaint into the UI](wiring_persists_through_faint.md) — call-site plumbing, the chart's progressive-time formula trick, badge-priority convention
- [Verifying without a browser tool](verification_without_browser_tool.md) — PARTLY SUPERSEDED (Playwright runs from Bash now); still the fallback build/serve/curl/node ladder and the scratch-script formula-proof technique
- [Sensitivity flip-bar + result-card share bar](sensitivity_flip_bar_and_share_bar.md) — two delta-vs-absolute bugs found, per-row independent scan axes, node strip-types' import-resolution ceiling
- [Shared trajectory-interpolation helpers](pattern_shared_trajectory_helpers.md) — DamageOverTimeChart.tsx owns ownDamageAt/teamContributionAt/totalAt; reuse, don't refork, for any new time-series view
- [Prefer cumulative totals over rate columns](feedback_cumulative_not_rate.md) — a per-row DPS/rate column decays toward zero once a candidate dies but elapsed time keeps growing; use running totals instead; row step later fixed to always-1s (superseded pickRowStep)
- [Weather + boss-moveset-sweep UI](feature_weather_and_boss_moveset_sweep.md) — full add-scenario-assumption checklist for weather, BossMovesetSweep.tsx, which species have 2+ charged moves to test with
- [Disable-mega-boost toggle + undefined-boost N/A convention](feature_disable_mega_boost_toggle.md) — candidateMegaBoostDisabled checklist, resolveBoost(species,disabled) helper, "N/A not 0" display rule, a real tsx cwd-resolution gotcha
- [Attack/Defense/Stamina IV sensitivity checks](feature_iv_sensitivity_checks.md) — copied Level check's exact pattern, runSustained gained ivs override, tsx now proven to clear the strip-types import ceiling
- [IV-input spinner + layout QA fixes](bugfix_iv_input_spinner_and_layout_qa.md) — narrow number-input spinner ate clicks and hid the digit; also grid/caption/flex-shrink fixes
- [Mega-boost copy said "your party", real mechanic boosts other trainers](fix_teammate_boost_copy_backwards.md) — copy-only fix across 5 files; uptime.ts doc comments flagged to engine-developer, not edited
- [Hide inert boost UI + move EPS/efficiency metrics](feature_hide_inert_boost_ui_and_move_efficiency_metrics.md) — anyBoostActive-gated party fields/columns, local hasActiveBoost (not imported, avoids circular import), MoveSelect EPS/DPS×DPE additions
- [Team Raid Simulator tab + tab-switcher scaffold](feature_team_raid_simulator_tab.md) — isMega validation-only gotcha, per-tier bossEffectiveHp, trajectories already stacked (no shared-helper reuse needed), normalizeTeamAssumptions pattern
- [weather.ts re-export gap](gap_weather_not_reexported.md) — RESOLVED 2026-09-06, engine now exports it directly; old workaround is obsolete, don't reintroduce it
- [Species Report tab (reverse lookup)](feature_species_report_tab.md) — ComparatorPrefill hand-off, why its Scenario codec lives in web not engine, scope cuts on bossStartsPrimed for multi-boss sweeps
- [Species Report sort-mode not serialized](bugfix_species_report_sortmode_not_serialized.md) — a display-only toggle is still a scenario field; audit other tabs' bare useState for the same blind spot
- [IV Breakpoints tab (fourth tab)](feature_iv_breakpoints_tab.md) — no standalone damage-modifier helper exists (replicate comparison.ts's inline construction), timeToFaint is fast-move-only (one dodge control, no boss charged move), empirically confirmed non-monotonic divergence
- [IV Breakpoints: active-boss sweep report + reversed table](feature_iv_breakpoints_active_boss_sweep.md) — SUPERSEDED by full-sweep-aggregate below; kept for the reversed-table/resolvedAttackerMoves history
- [IV Breakpoints: full-species-sweep tallied verdict](feature_iv_breakpoints_full_sweep_aggregate.md) — replaced per-boss table with one paragraph; exact win-tally definition; don't hardcode species count (drifted to 1081)
- [IV Breakpoints: tier-filtered headline + per-tier table](feature_iv_breakpoints_tier_filtered_headline.md) — RAID_TIER_NUMERIC map (RaidTier has no numeric field), headline scoped to tier 4+; "narrow practical effect" claim is STALE, see fix below
- [IV Breakpoints: fixed 35-50 level range](feature_iv_breakpoints_levels_35_to_50.md) — narrowed both compareIvSpreads call sites from engine's 1-50 default via one LEVELS_35_TO_50 constant, no Scenario change needed
- [IV Breakpoints: per-rarity default tier fix](fix_iv_breakpoints_per_rarity_default_tier.md) — defaultRaidTierForSpecies wired into sweep bucketing; caveat prose rewritten, filter now excludes 919/1081 not ~7; speciesRegistry is an instance not a factory
- [Attack/Defense Breakpoints tab (fifth tab) + move-type colors](feature_attack_defense_breakpoints_tab_and_move_type_colors.md) — dodge scoped OUT (no time axis to apply it to), color+text-tag chosen over icon, bossEffectiveStats needs live tier too, a degenerate all-flat sheet found to be correct not buggy
- [Shadow toggle on all 4 tabs' own-attacker pickers](feature_shadow_toggle_all_tabs.md) — shared shadowToggle.ts, raw-species-until-engine-boundary rule (avoids self-locking checkbox), breakpoints.ts's raw-number blind spot, Scenario/TeamScenario extension-type trick for engine-owned types
- [Mobile 375px horizontal-overflow fix, two causes](bugfix_mobile_horizontal_overflow.md) — unwrapped .tab-switcher nav + 2-of-6 .time-series-table sites missing the overflow-x wrapper div; why not overflow-x on .panel directly
- [Species Report: past raids + tier filter + 3 fixes](feature_species_report_past_raids_and_tier_filter.md) — includedTiers null-self-heals, filter-targets-not-rows for perf, actualTierUsed fallback-display fix, badge reuse for raid-history provenance
- [Species Report: debounced sweep + pogoapi-previous source](feature_species_report_debounced_sweep_and_pogoapi_previous.md) — debounce a narrow sortMode-excluded object not whole state, whole-object (not per-field) debounce avoids a torn species/move read, inverted tier-resolution for the new source, a heading/count staleness mismatch found post-debounce
- [Species Report: era-HP wiring + a 4th unaccounted-for raid-history source](feature_species_report_era_hp.md) — bossMaxHpOverride/eraHp plumbed through, found+fixed a concurrently-landed "bulbapedia-archive" source two branches keyed on the wrong literal; its "shared guard drives both override+badge" claim is now PARTLY SUPERSEDED, see next entry
- [Species Report: pokebattler-legacy 5th raid-history source](feature_species_report_pokebattler_legacy_source.md) — check-raid-history-sources' 4-site checklist, an else-branch that was mechanically correct but had stale tooltip prose, eraHp structurally never set for this source
- [Multi-flip crossing marker bug](bugfix_multiflip_crossing_marker.md) — chart marked the FIRST lead-flip, finalLeader used the last totals independently; fixed to derive finalLeader from the LAST flip so they can't disagree; RNG-not-reseeded-per-call gotcha in multi-call scratch scripts
- [Boss cadence toggle (fixed-interval vs energy-driven)](feature_boss_cadence_toggle.md) — extension-type trick still needed even after the engine field lands (only the 3 *Inputs interfaces got it, not Scenario/TeamScenario), shared bossCadence.tsx over per-tab duplication for sourced prose, a transient unrelated concurrent-engine-edit tsc error, SpeciesRegistry needs a register() loop not a constructor arg
- [Power-Up Optimizer tab (sixth tab)](feature_power_up_optimizer_tab.md) — 3-way mega/shadow/purified exclusion order (mega+purified is a legal real combo, proven not assumed), optimizePowerUps measured at 217ms/202 candidates (far cheaper than feared), null-vs-zero efficiency sorting, clamp-at-call-boundary-not-state convention
- [Power-Up Optimizer: 20-seed noise floor](feature_power_up_optimizer_noise_floor.md) — polled a mid-landing engine contract to full completion not just first grep hit, 3-group sort (measurable gain / within-noise / measurable loss), OPTIMIZER_ITERATIONS deliberately not a Scenario field, vite preview doesn't apply the GH Pages base path
- [Power-Up Optimizer: fixed-budget plan section](feature_power_up_optimizer_fixed_budget_plan.md) — reused same inputs object for optimizePowerUps+planPowerUpBudget, ~800ms combined (no gating toggle needed), Playwright WAS available this session — a real share-URL browser check beats CLI-only
- [Power-Up Optimizer: bestBlockedCandidate callout](feature_power_up_optimizer_blocked_candidate_callout.md) — "blocked, not done" vs "genuinely optimal", all shortfalls joined not [0], exported sentence fn shared with CLI, 2nd proof concurrent-session scratch files cause transient tsc failures outside packages/engine too

- [run/*.ts extraction + vitest + run-scenario CLI](feature_run_module_extraction_and_cli.md) — the aliasing trick that made six extractions low-risk, debounce-splitting gotcha, a real effectiveIsShadow(null,true) edge case, tsconfig.scripts.json needing jsx+DOM lib
- [Disk-full blocker on Playwright task, RESOLVED same day](blocker_disk_full_playwright_task.md) — check free space before any future browser-install/build-heavy task here
- [Playwright e2e smoke suite shipped](feature_playwright_e2e_suite.md) — registry.ts's bare JSON imports break under Playwright's Node loader (not Vite/Vitest); tsx-subprocess workaround, no-test-id selector convention confirmed sufficient
- [Shared WeatherSelect icon control](feature_weather_select_shared_icon_component.md) — replaced 6 copy-pasted weather `<select>`s; primitives-only hand-rolled SVG icons (no bezier), "none" = sunny + fixed-red slash not currentColor
- [Default dodge to Perfect + Comparator per-candidate override](feature_default_perfect_dodge_and_candidate_override.md) — checklist wiring onto an already-shipped engine field, sensitivity.ts also needed the override threaded through, found a real pre-existing engine fast-attack-dodge lockout bug (flagged, not fixed)
- [SpeciesPicker `primary` sizing prop](feature_species_picker_primary_sizing.md) — opt-in modifier class not a global `.species-picker` restyle, since 6+ Team Raid slots share the same component
- [Whole-app visual redesign pass](feature_visual_redesign_pass.md) — Playwright IS drivable from Bash (screenshots + Read = real visual QA, supersedes the no-browser ladder); JSX ate a space in the flip sentence; .tab-switcher is shared by 3 controls; .table-scroll replaces 8 inline wrappers
- [Roster Optimizer Phase 1: Poke Genie CSV import](feature_roster_optimizer_phase1_csv_import.md) — mega-form imageUrl-dex trap (must resolve by base-species name, not dex), segment-anchored form-token matching, compact-vs-hydrated pool storage, cross-checking git status against a parallel session, BOM-in-literal lint/Edit-tool gotcha, FastMove type omits energyCost/vulnerableWindowSeconds, Playwright scratch scripts must resolve from inside packages/web
- [Boss cadence: third "energy-gated-interval" value](feature_energy_gated_interval_cadence.md) — re-export-not-redeclare of an engine-landed union, `export type {X} from` doesn't bring X into local scope, scratch tsx scripts must live inside the repo (not OS temp) to resolve `@pogo-analyzer/engine`, CLI-proof of a share URL beyond mere round-trip
- [Multi-raid Roster Optimizer Phase 3a](feature_multi_raid_roster_optimizer_phase3a.md) — mode switch + BossSetPanel + runRosterPlanner.ts, §3.1's bossIds-authoritative precedence, RosterImportPanel lifted to controlled props, a real Playwright disk-pressure false-positive (fixed by `--workers=1`, not the diff)
- [Multi-raid Roster Optimizer Phase 3b](feature_multi_raid_roster_optimizer_phase3b.md) — Web Worker split + TS webworker-lib conflict avoided via cast, dedupe's exact-key/closest-to-mean pattern, mega candy family closed by pure suffix convention (61/61), per-boss drill-down was pure UI (data already existed)
- [Multi-raid Roster Optimizer Phase 4 (web)](feature_multi_raid_roster_optimizer_phase4_ui.md) — worker's 2nd request type needs zero remapping (RosterBudgetInputs structurally accepts RosterPlannerInputs), one click drives two independently-tracked async calls, generic runOnWorker/runMultiRaidTrackedComputation helpers, a real bash `--` gotcha with tsx
- [Refactor: PowerUpOptimizerView single-raid extraction](refactor_powerup_optimizer_single_raid_extraction.md) — mirrored multi-raid's 2-component split but kept budget-plan INLINE (order-sensitive), plain-prop closures don't need `!` the way `result.data!` did, found+preserved (not fixed) a real thead-tooltip asymmetry the task wrongly called "verbatim"
- [Collapsible sections across all 6 tabs](feature_collapsible_sections.md) — heading nested INSIDE `<summary>` (not beside it), why that breaks e2e `xpath=..` (fix: `xpath=ancestor::details[1]`), a real Bash-heredoc Windows-path-mangling bug found while verifying, before/after page-height numbers
- [Species Report: header-wrap modifier class + Boss HP label removal](feature_species_report_header_wrap_and_hp_label_removal.md) — per-tab wrap needs a modifier class + per-column ch max-widths (white-space:normal alone doesn't wrap in auto-layout), hyphen breaks are free/no overflow-wrap needed, concurrent-editing discipline (Edit-only on shared styles.css, ignore another agent's in-flight tsc errors)
- [Team Raid: "more detailed assumptions" toggle + derived boss frequency](feature_team_raid_detailed_assumptions_toggle.md) — non-contiguous gated-field group resolution, run-module-owned derivation (0 starting energy, Infinity/0 fallback), seed-on-check convention, a 2nd inverted-default case + why swapCost/reviveCost's `?? 0` stayed put
- [Bugfix: multi-raid optimizer ignored rankBy + Species Report undercount](bugfix_multiraid_rankby_and_species_report_undercount.md) — shared powerUpCandidateSort.ts extracted from single-raid's inline sort (2 candidate shapes, same 3-group comparator), sort-after-dedup not before, three concurrent unrelated sessions writing the SAME repo/memory dir simultaneously and how to tell their errors from your own
- [Mega Level dropdown + "+" move badges, all six tabs](feature_mega_level_all_six_tabs.md) — MegaLevelSelect/PlusMoveBadge shared components, TeamRaidSlotInput's null-vs-undefined gotcha, gate on species.boost not hasActiveBoost (different mechanic), multi-raid's roster-wide setting shipped honestly inert (rosterPlanner.ts has zero channel), real synced "+" move found mid-task for end-to-end proof
