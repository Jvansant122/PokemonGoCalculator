---
name: pass-2026-09-14c
description: Fourth meta-researcher pass same day — verified all three pass-14 recommendations shipped (PreToolUse scratch-block hook, engine-developer body edit, post-edit.mjs docs-drift branch), then found CLAUDE.md's own "two hooks" line is now stale as a direct result; 2 recommendations (1 CLAUDE.md correction, 1 hook-trigger widening), operator heredoc friction judged not a finding
metadata:
  type: project
---

Fourth pass, same day as [[pass_2026-09-14]] and [[pass_2026-09-14b]]. HEAD `14fde1d`, tree clean,
one commit landed since pass 3 (a HANDOFF.md/IDEAS.md docs rewrite — confirmed via the invocation,
not independently re-verified with `git log` since I have no Bash tool this pass).

**CHANGELOG re-fetched** (`raw.githubusercontent.com/anthropics/claude-code/main/CHANGELOG.md`,
2026-09-14): still flat at **2.1.270**, only bullet is the 2.1.269 Bash-permission regression fix.
Fourth consecutive pass with nothing new on the platform side.

**Bookkeeping first — closed the loop on pass 2's three recommendations, all adopted:**
read `.claude/settings.json`, `.claude/hooks/block-engine-test-scratch.mjs`, `.claude/hooks/post-edit.mjs`,
and `.claude/agents/engine-developer.md` directly. All three shipped in `56d0bbc` per `HANDOFF.md`:
the `PreToolUse`/`Write` scratch-block hook exists verbatim as designed; `post-edit.mjs` gained the
`isDocsSurface` branch running `check-docs-drift`; `engine-developer.md`'s body carries the
fodder-boss `timeToClearSeconds` trap. Pass 3's `typecheck:scripts` recommendation (isWebSrc branch)
is still NOT adopted — confirmed by reading `post-edit.mjs` line 47, still only
`npm run typecheck:web`. Consistent with `meta_ideas.md`'s "Proposed... Taken" note; not re-proposed.

**Recommended, pending meta-architect action:**

1. **`CLAUDE.md`'s "Skills and hooks" section is now stale, and the drift is a direct side-effect
   of adopting pass 2's own recommendation #1.** It reads (fetched into my context this session,
   current HEAD): *"`.claude/settings.json` has two hooks. `SessionStart` runs
   `.claude/hooks/session-start.sh`... `PostToolUse` on `Edit|Write` runs
   `.claude/hooks/post-edit.mjs`, which reruns the cheapest check that owns the edited file —
   `npm run test:engine`... the web type-check... `npm run check-scenario-roundtrip`..."* This is
   wrong on two counts, both verified by reading `.claude/settings.json` directly this session:
   (a) there are now **three** hooks, not two — a `PreToolUse`/`Write` matcher running
   `.claude/hooks/block-engine-test-scratch.mjs` was added in `56d0bbc` (2026-09-14) and is
   completely unmentioned; (b) `post-edit.mjs`'s own branch list has grown to **four** (its own
   header comment lists `test:engine` / `typecheck:web` / `check-scenario-roundtrip` /
   `check-docs-drift`), but CLAUDE.md's prose still names only the original three and omits the
   docs-drift branch entirely — the same branch that pass 2's own recommendation #3 added. Source:
   direct read of `.claude/settings.json` (three top-level hook matchers: `SessionStart`,
   `PreToolUse`, `PostToolUse`) and `.claude/hooks/post-edit.mjs`'s header comment and `isDocsSurface`
   regex, both 2026-09-14, both current HEAD (`14fde1d`, clean tree) — not platform docs, pure
   repo-state comparison. **Lands in CLAUDE.md**, the single most expensive location (loaded every
   session and re-read by every subagent per this project's own stated cost model). Cost of the fix
   itself is not a new ongoing cost — it corrects an already-paid-for paragraph to match what's
   actually true, which matters because the current text actively teaches the next session/agent an
   incomplete and now-wrong mental model of what governs every `Write` call (the scratch-block deny
   is currently invisible to anyone who only reads CLAUDE.md). Recommend rewriting the paragraph to
   state three hooks, name `block-engine-test-scratch.mjs` and what it denies, and list
   `post-edit.mjs`'s four branches. Nothing to retire — this is a correction, not new machinery.

2. **`post-edit.mjs`'s `isDocsSurface` regex should also match `.claude/settings.json` and
   `.claude/hooks/**/*.mjs`.** This is the structural reason finding #1 was possible at all: the
   hook that automatically reruns `check-docs-drift` after a docs-surface edit
   (`.claude/agents/*.md`, `.claude/skills/**/SKILL.md`, `CLAUDE.md`, `HANDOFF.md`) does not fire
   when the file edited is `.claude/settings.json` or a `.claude/hooks/*.mjs` script itself — so
   the commit that added the `PreToolUse` hook (`56d0bbc`, which touched `settings.json` and added
   `block-engine-test-scratch.mjs`) triggered zero automatic check, and CLAUDE.md's hook-count
   paragraph went stale silently. This is the same shape as pass 3's `typecheck:scripts` finding —
   the checking mechanism already exists (`check-docs-drift` presumably could be taught to compare
   hook counts, though I did not verify it currently does — see the flag below) but isn't *invoked*
   at the file class that actually changes hook behavior. Confirmed by reading
   `.claude/hooks/post-edit.mjs`'s `isDocsSurface` line directly — it lists exactly four path
   patterns (`agents/*.md`, `skills/**/SKILL.md`, `CLAUDE.md`, `HANDOFF.md`), none of which match
   `settings.json` or `hooks/*.mjs`. **Cost**: hook script only, zero ongoing context-window cost;
   the only price is one more bounded `check-docs-drift` run on the rare occasion someone edits
   `.claude/settings.json` or a hook script. Nothing to retire — additive to an existing branch.

   **Flagged but not counted as a third recommendation** (matches this project's own precedent from
   [[pass_2026-09-14]] of naming a `check-docs-drift.mjs` internals gap without formally proposing
   the fix): even with #2 wired in, I did not verify `check-docs-drift.mjs` actually contains a
   structural check comparing CLAUDE.md's stated hook count/contents against `settings.json`'s real
   hooks — it may only catch this class of drift if such a check exists. If it doesn't, wiring #2
   alone would still let this exact staleness recur silently next time a hook is added. That's a
   `scripts/` implementation question, outside my remit — flagging for whoever next touches
   `scripts/check-docs-drift.mjs` (`data-sync` or the user directly), same disposition as the prior
   flag on that script's prose-parsing gap.

**Considered and dropped:**
- **The operator heredoc/quoting friction named in this pass's invocation** (Bash `cat > file
  <<'EOF'` combined with `node -e '...'` in one call fails to parse; embedded double-quotes inside
  `node -e '...'` break the JS parse) — judged **(b), not a finding**. The `Write`/`Edit` tool
  descriptions available to every agent already state the correct answer verbatim: "Prefer the Edit
  tool for modifying existing files — it only sends the diff... Only use [Write] to create new
  files or for complete rewrites" (tool schema text, loaded in every session already, not something
  I fetched — it is the tool's own description). Grepped `CLAUDE.md` for `heredoc`/`Edit tool`/
  `node -e` — zero matches, confirming the project has never needed to restate this because it's
  not a repo-specific convention; it would read identically in any other repo with a Bash tool and
  an Edit tool both available, which fails this pass's own "this exists" bar. This is the same
  shape as `REJECTED_IDEAS.md` #16 (the right mechanism already existed and was bypassed) — not
  a tooling gap, an in-the-moment tool choice. No CLAUDE.md line, hook, or skill would fix a
  bypass of an already-loaded instruction without adding permanent cost for a problem that isn't
  repo-specific.

**Rejected/open from prior passes, not re-litigated:** `gh` CLI still not installed (open, not
rejected — not re-checked this pass); GitHub MCP server, `/skill-doctor`, browser MCPs — all still
correctly out per [[pass_2026-09-12]]'s arithmetic.

**Next pass**: if CHANGELOG is still flat at 2.1.270 next time (would be five consecutive passes),
worth widening the net to release notes / blog per pass 3's own note, since the raw CHANGELOG.md
may simply lag actual releases. Otherwise: re-check whether meta-architect acted on #1/#2 above,
and whether `check-docs-drift.mjs` gained a hook-count structural check (resolves the flag).
